## Quick start (Linux/Mac)

git clone <repo-url>
cd playwright-parabank-automation
npm ci
npx playwright install --with-deps
npx playwright test

## Quick start (Windows PowerShell)

git clone <repo-url>
cd playwright-parabank-automation
npm ci
npx playwright install --with-deps
npx playwright test
